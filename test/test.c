/**
	* \file test/test.c
	* \brief Test de l'objet test qui sert à 'Ceci est un super test'.
	* \author Erwan PECHON
	* \version 0.1
	* \date Jeu. 02 Févr. 2023 12:42:57
	*
	* Test de l'objet test qui sert à 'Ceci est un super test'.
	*
*/

// INCLUSION(S) DE(S) BIBLIOTHEQUE(S) NÉCÉSSAIRE(S)
#include "../lib/test.h"

// CRÉATION(S) DE(S) CONSTANTE(S) NUMÉRIQUE(S)

// CRÉATION(S) D(ES) ÉNUMÉRATION(S)

// CRÉATION(S) D(ES) STRUCTURE(S) ET D(ES) UNIONS(S)

// CRÉATION(S) DE(S) CONSTANTE(S) DE STRUCTURE(S)

// CRÉATION(S) DE(S) FONCTION(S)


// PROGRAMME PRINCIPALE
	/* Programme qui 'Ceci est un super test' */
int main() {
	// INITIALISATION DE(S) VARIABLE(S)
	test_t obj;

	// INSTRUCTION(S)
	// FIN DU PROGRAMME
	printf("\n\n\t\tFIN DU TEST\t\t\n\n");
	return 0;
}
	/* Programme qui 'Ceci est un super test' */
// PROGRAMME PRINCIPALE

// #####-#####-#####-#####-##### FIN PROGRAMMATION #####-#####-#####-#####-##### //

